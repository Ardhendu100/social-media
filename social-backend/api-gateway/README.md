# API Gateway

This service acts as the entry point for all client requests. It handles authentication, routes requests to other microservices, and aggregates responses as needed.

- Built with Django
- Will be containerized with Docker
- Part of the social media backend monorepo
